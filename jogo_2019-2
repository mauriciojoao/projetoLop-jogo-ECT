var x = 200;
var y = 200;
 
function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(0);
 
  if (keyIsDown(RIGHT_ARROW)){
    x = x + 3;
  
  }
  if (keyIsDown(LEFT_ARROW)){  
    x = x - 3;
   
  }
  ellipse( x, y, 170, 170);
}
